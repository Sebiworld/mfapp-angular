import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.musical-fabrik.mfapp',
  appName: 'mfapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
